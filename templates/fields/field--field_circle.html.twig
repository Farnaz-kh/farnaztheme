<div class="row justify-content-center">
    {% for item in items %}
    <div class="col-sm-3 my-3">
        <div class="item_1 text-center py-5 text-white bg-secondary">
            {{item.content}}
        </div>
    </div>
    {% endfor %}
</div>